cerberus (0.12.0) unstable; urgency=medium

  Authorization gets more expressive and observability gets easier to reach,
  without either loosening a default.

  * allowed_principals entries may now be a `requested: issued` map, not just a
    plain name. A group can map `root: global-root`, so a user asks for root and
    receives a certificate whose only principal is global-root. Casbin policy is
    keyed on the requested name alone, so a mapping target is not requestable
    unless a plain entry or `*` also covers it, and a mapped certificate never
    carries the requested name alongside the issued one. Translation happens in
    authz.Authorize; the /sign handler still derives principals only from the
    authorization result, never from the request body.
  * cssh survives requested != issued. Because a mapped certificate's principal
    list is no longer a record of what was asked for, cssh records
    `<serial> <policy-fingerprint> <requested-set>` in `<privkey>-cert.requested`
    and compares against that. Upgrade the client BEFORE enabling mapping on the
    server: a cssh without the sidecar re-signs on every invocation instead of
    reusing its cache.
  * Policy changes invalidate client caches. config.PolicyFingerprint() (a
    SHA-256 over groups, strip_realms and self_principal only -- never keytab,
    TLS, LDAP or OAuth material) is returned on every /sign success and by the
    new authenticated, limiter-exempt GET /policy. cssh probes it only after
    every local check says "reuse" and never prompts, so a certificate issued
    under a superseded policy cannot ride out its validity window.
  * New optional plain-HTTP listener for /health and /metrics, for scrapers and
    load balancers that cannot present the CA's TLS trust. Configure it under
    `observability_http:`; it is off by default, binds 127.0.0.1 rather than all
    interfaces, and listens on 9109. Both endpoints remain on the HTTPS listener
    as well -- this adds a route to them, it does not move them. The listener
    builds its own mux carrying only those two paths, so /sign and /policy are
    absent from it entirely and no Kerberos ticket or OIDC bearer token can be
    sent to it in cleartext. A non-loopback bind logs
    config.observability_http.exposed at startup, and a port equal to the one in
    `listen` is refused at config load rather than surfacing as an opaque
    "address already in use".
  * The cssh certificate cache is now tested. Ten cases drive the real cssh
    function against real ssh-keygen-minted certificates under both bash and
    zsh, covering the sidecar's v1 and v2 record formats, the serial-mismatch
    fallback, and the removal of a record that could not be refreshed. Running
    both shells found a bug immediately: an EXIT trap set inside a function is
    function-scoped in zsh but shell-scoped in bash.

 -- Paul Kilar <pkilar@gmail.com>  Fri, 04 Sep 2026 14:43:01 -0400

cerberus (0.11.0) unstable; urgency=medium

  Packaging is the theme of this release: the container build layer, automatic
  EIF packaging, and two repairs to build entry points that were broken.

  * build-rpm.sh was BROKEN on 0.10.6 and is repaired. 0.10.6 added Source1 and
    Source2 (the sysusers.d fragments) to the spec but the script never staged
    them into SOURCES/, so rpmbuild failed with "cannot stat". The script now
    derives the list from the spec itself, so adding a Source3 later cannot
    silently break it again.
  * New multi-distro container build layer: packaging/build-in-container.sh
    builds a target's packages in a clean container and optionally lints them,
    driven by packaging/targets.tsv (fedora, rhel9, rhel10, amazonlinux2023,
    debian-bookworm, arch). The container supplies the distribution and the host
    supplies the architecture -- nothing is emulated or cross-compiled, which is
    what keeps dependency generation correct. Exposed as make package-<target>,
    make lint-package-<target> and make packages-all.
  * The cerberus-signer-eif package is now built automatically when the working
    tree holds the CA key material the EIF is made from (ssh-cert-signer/
    ca_key.enc and ca_key.pub), rather than only when --eif was passed. A tree
    without them -- CI, upstream, a clean checkout -- is unaffected and builds
    exactly the packages it always did. --no-eif opts out; --eif still names an
    EIF explicitly. Detection is shared by all three packagings via
    packaging/eif-detect.sh so it cannot drift between them.
  * The build now REFUSES a ca_key.enc that is really a plaintext private key.
    make encrypt-ca-key preserves the plaintext when the KMS call fails, so a
    tree can hold one, and nothing downstream re-checked -- it would have been
    baked into the EIF and published inside the package.
  * The PCR manifest (pcr-manifest-<arch>.json) is saved alongside the built
    packages, and collected into dist/<target>/<arch>/ by the container build
    driver. A PCR-conditioned KMS key policy must be updated to the new PCR0
    before the new EIF is deployed.
  * make eif builds the EIF for the host architecture instead of attempting
    both. An EIF can only be built on hardware of the architecture it targets,
    so the previous eif-amd64 + eif-arm64 default always produced one image and
    one error; off-native targets now refuse with an explanation instead of
    failing inside nitro-cli. The enclave binary is no longer cross-compiled for
    the architecture this host cannot package (make build-all still does both).
  * .gitignore now covers the bare plaintext ca_key, not just ca_key.*, which
    ssh-keygen -f ca_key and a failed encrypt-ca-key both leave behind.
  * Built with Go 1.27. No distro ships it yet, so the RPM and Debian recipes
    set GOTOOLCHAIN=auto to fetch it, overriding the GOTOOLCHAIN=local baked
    into the official golang images. The binaries remain statically linked with
    no new library dependencies.
  * Dependency refresh across all three modules: AWS SDK v2 (config 1.32.40 ->
    1.33.1, KMS 1.56.0 -> 1.57.1 plus the indirect set), golang.org/x/net
    0.57.0 -> 0.58.0, x/sync 0.21.0 -> 0.22.0, protobuf 1.36.11 -> 1.36.12,
    prometheus client_model/common/procfs, and fxamacker/cbor 2.9.2 -> 2.9.3 in
    the signer's CMS envelope parser. govulncheck reports no vulnerabilities.
  * Documentation: the RPM supported-distribution claim no longer lists RHEL 8,
    which is EOL. Supported targets are Amazon Linux 2023, Fedora and RHEL 9+.

 -- Paul Kilar <pkilar@gmail.com>  Wed, 02 Sep 2026 13:58:18 -0400

cerberus (0.10.6) unstable; urgency=medium

  * Repair the 0.10.5 changelog entry, which was missing its trailer line. dpkg
    parsed it with no Date or Maintainer field and warned, and lintian reported
    syntax-error-in-debian-changelog against all four binary packages, each of
    which ships this file as changelog.gz.
  * RPM-side fix carried in the same release for version parity: the RPM
    packages were uninstallable on rpm >= 6 (current Fedora) because %files
    entries naming the cerberus/cerberus-audit accounts generate
    Requires: user()/group(), and only a packaged sysusers.d file provides them.
    The RPM now ships sysusers.d fragments as Debian already did. No change to
    the Debian packages themselves.
  * Committed version fields in the RPM spec and the Arch PKGBUILD are now
    obvious placeholders rather than stale real-looking versions, so a build run
    without the wrapper scripts fails visibly instead of producing a package
    labelled with a previous release.
  * Dependency updates: aws-sdk group bumped in ssh-cert-api and
    ssh-cert-signer.

 -- Paul Kilar <pkilar@gmail.com>  Mon, 31 Aug 2026 12:00:00 -0400

cerberus (0.10.5) unstable; urgency=medium

  * Ship the config template at /usr/share/cerberus/config.yaml.example
    instead of /etc/cerberus/config.yaml.example. Nothing reads the file --
    operators copy it to config.yaml -- so shipping it under /etc made it a
    dpkg conffile, which froze it: once an admin touched it, upgrades stopped
    refreshing it and newly added options never surfaced. As read-only package
    data under /usr/share it is replaced on every upgrade, matching the RPM
    and Arch packaging. cerberus-api.maintscript retires the old conffile;
    a locally modified copy is preserved as .dpkg-bak.
  * The template now ships 0644 rather than 0640. A restricted mode is
    anomalous under /usr/share and bought nothing here: the file carries no
    secrets and is public in the source repository. Its lintian override is
    dropped accordingly; the env files keep 0640 and their override.
  * cerberus-api continues to own /etc/cerberus (0750 root:cerberus), which
    was previously created only as a side effect of installing the template
    into it. That is still where the operator's config.yaml belongs.

 -- Paul Kilar <pkilar@gmail.com>  Mon, 17 Aug 2026 18:00:00 -0400

cerberus (0.10.4) unstable; urgency=medium

  * Reduce cerberus-vsock-watch alert noise on a cerberus-api.service
    restart: a third real restart chaos test on the same RHEL 10 host
    confirmed 0.10.3's Indeterminate downgrade was working correctly
    (the reported failure was a bug in the verification script, since
    fixed) but the Indeterminate alert still fired on essentially every
    restart -- safe but noisy. Allowlist.Classify's single cgroup
    recheck is now a bounded retry loop instead of one attempt, so the
    common case resolves cleanly to Expected with no alert at all. See
    docs/vsock-connect-detection.md §4.1.

 -- Paul Kilar <pkilar@gmail.com>  Thu, 23 Jul 2026 17:30:00 -0400

cerberus (0.10.3) unstable; urgency=medium

  * Fix a second round of a real false-positive in cerberus-vsock-watch,
    found via a second real cerberus-api.service restart chaos test on a
    real RHEL 10 host: 0.10.2's uncached recheck reduced but did not
    fully close a cgroup-mismatch false positive, since a single
    immediate recheck can still race systemd's own cgroup settling
    during a restart. Allowlist.Classify now downgrades a cgroup
    mismatch that survives the recheck to Indeterminate (alerts, never
    auto-kills) instead of trying to outrun the timing race. Applies to
    this Debian package the same as RPM and Arch. See
    docs/vsock-connect-detection.md §4.1.

 -- Paul Kilar <pkilar@gmail.com>  Thu, 23 Jul 2026 16:00:00 -0400

cerberus (0.10.2) unstable; urgency=medium

  * Fix a real false-positive in cerberus-vsock-watch found via a live
    cerberus-api.service restart chaos test on a real RHEL 10 host
    (verify-vsock-watch-hardware.sh): Allowlist's cached expected cgroup
    inode can go stale across a restart that changes the unit's cgroup,
    misclassifying the newly-restarted, legitimate ssh-cert-api as
    Anomalous for up to the 5s cache window -- and, with the opt-in
    --block reactive-kill enabled, killing it outright. Allowlist.Classify
    now does one uncached re-check before declaring a uid or cgroup
    mismatch Anomalous. Applies to this Debian package the same as RPM
    and Arch, since it's a Go code fix, not packaging. See
    docs/vsock-connect-detection.md §4.1.

 -- Paul Kilar <pkilar@gmail.com>  Thu, 23 Jul 2026 14:30:00 -0400

cerberus (0.10.1) unstable; urgency=medium

  * No Debian-specific changes; version bump to stay in lockstep with the
    RPM/Arch packaging, which fixed cerberus-vsock-watch missing auditctl on
    RHEL 10 (that release moved it into a separate audit-rules package). The
    Debian cerberus-vsock-watch package's Depends: audit is unaffected.

 -- Paul Kilar <pkilar@gmail.com>  Thu, 23 Jul 2026 09:00:00 -0400

cerberus (0.10.0) unstable; urgency=medium

  * New cerberus-vsock-watch package: a detective control for SIGN-1
    (docs/THREAT-MODEL.md) — alerts on any AF_VSOCK connect() to the enclave
    from a process other than cerberus-api, via two independent detectors
    (an auditd rule and an eBPF tracepoint probe). Does not close the
    underlying gap; makes exploitation observable. See
    docs/vsock-connect-detection.md.

 -- Paul Kilar <pkilar@gmail.com>  Wed, 22 Jul 2026 09:50:00 -0400

cerberus (0.9.0) unstable; urgency=medium

  * LDAP backends can now discover servers via DNS SRV records (srv: block with
    tls_mode) and fail over across them per RFC 2782, in addition to a fixed
    url:.

 -- Paul Kilar <pkilar@gmail.com>  Fri, 10 Jul 2026 20:50:19 -0400

cerberus (0.8.0) unstable; urgency=medium

  * Initial Debian packaging, mirroring the RPM and Arch packaging. Multi-binary
    source package producing:
    - cerberus-api: the host-side HTTPS gateway service.
    - cerberus-signer: the Nitro Enclave signer service (binary, wrapper, unit).
    - cerberus-client: the cssh shell wrapper (Architecture: all).
    - cerberus-signer-eif: opt-in, built only under the pkg.cerberus.eif build
      profile; bundles a prebuilt EIF carrying per-deployment CA key material.
  * Service env files live in /etc/default/; the cerberus user and
    /var/log/cerberus are created via sysusers.d/tmpfiles.d; units are installed
    without being enabled or started (they need site config and a Nitro host).

 -- Paul Kilar <pkilar@gmail.com>  Thu, 09 Jul 2026 18:00:00 -0400
